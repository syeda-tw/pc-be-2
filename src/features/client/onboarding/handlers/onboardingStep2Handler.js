const onboardingStep2Handler = (req, res) => {
    console.log("Onboarding Step 2 Handler");
    res.status(200).json({ message: "Onboarding Step 2 Handler" });
};

export default onboardingStep2Handler;
