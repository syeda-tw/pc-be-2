const onboardingStep3Handler = (req, res) => {
    console.log("Onboarding Step 3 Handler");
    res.status(200).json({ message: "Onboarding Step 3 Handler" });
};

export default onboardingStep3Handler;
